# Trade Lane Risk Monitor — Python dependencies
# Install with:  pip install -r requirements.txt

Flask==3.1.3            # web server for the dashboard (Phase 3)
requests==2.34.2        # HTTP calls to all the data APIs (Phase 1)
python-dotenv==1.2.2    # loads API keys from .env (Phase 1)
feedparser==6.0.12      # parses the Google News RSS fallback (Phase 1)
gunicorn==26.0.0        # production WSGI server (for deployment)

# Note: risk scoring (Phase 2) and the SQLite store use only the Python
# standard library (statistics, math, json, sqlite3) — no extra packages.
